# ============================================
# HYTALE MCP SERVER - ENVIRONMENT CONFIGURATION
# ============================================

# Port where the server will listen
# Default: 3000
PORT=3000

# Path to the documentation build directory
# This directory must contain:
#   - class_lookup.json (class index)
#   - *.md files (markdown documentation)
# Default: ../build (relative to project root)
DOCS_DIR=../build

# Logging level
# Options: error | warn | info | http | verbose | debug | silly
# Default: info
LOG_LEVEL=info

# ============================================
# MONITORING CONFIGURATION
# ============================================

# Loki (Logs)
# Enable direct push to Loki
# When enabled, logs are sent directly to Loki
# Console logging is disabled in production when Loki is enabled
# Default: false
ENABLE_LOKI_PUSH=false

# Loki server URL
# Example: http://localhost:3100 or http://loki:3100
LOKI_URL=

# Prometheus Pushgateway (Metrics)
# Enable direct push to Pushgateway
# When enabled, /metrics endpoint is not needed
# Metrics are pushed every METRICS_PUSH_INTERVAL milliseconds
# Default: false
ENABLE_METRICS_PUSH=false

# Pushgateway server URL
# Example: http://localhost:9091 or http://pushgateway:9091
PUSHGATEWAY_URL=

# Metrics push interval in milliseconds
# Default: 10000 (10 seconds)
METRICS_PUSH_INTERVAL=10000

# ============================================
# DOCUMENTATION DOWNLOAD CONFIGURATION
# ============================================

# Pre-signed URL to download documentation ZIP archive
# Leave empty to skip auto-download and use local DOCS_DIR
# The server will automatically detect URL changes and re-download when needed
# Example: https://minio.example.com/docs/latest.zip?X-Amz-Algorithm=...
DOCS_URL=

# Download timeout in milliseconds
# Default: 300000 (5 minutes)
DOCS_DOWNLOAD_TIMEOUT=300000
